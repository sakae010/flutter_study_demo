import 'package:flutter/material.dart';

///演示提示弹框
class BubbleDemoPage extends StatelessWidget {
  const BubbleDemoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
